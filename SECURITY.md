# 安全说明

## 支持范围

项目当前处于 `0.x` 阶段，只维护最新主分支。发现文档站或实验代码的安全问题时，请先通过仓库维护者提供的私有联系方式报告，不要在包含利用细节的公开 Issue 中披露。

## 本地文档服务

`npm run docs:dev` 只监听 `127.0.0.1`，用于本机文档预览。不要把开发服务器暴露到公网或不受信任的局域网；对外发布应部署 `npm run docs:build` 生成的静态文件。

截至项目首版验证时，VitePress 最新稳定版 `1.6.4` 的开发依赖 Vite/esbuild 仍存在 npm 安全公告，并且上游暂未提供兼容修复版本。相关公告影响开发服务器，不影响 Maven 实验，也不会把开发服务器打包进静态站点。上游发布兼容版本后应优先升级并重新执行 `npm audit` 与静态构建。

