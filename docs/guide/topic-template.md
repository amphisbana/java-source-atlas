# 新专题模板

复制下面的结构创建专题首页，再根据复杂度拆分调用链、状态机、版本差异和 Debug Lab 页面。内容不完整时可以先提交专题提案，不要用占位结论凑齐章节。

````markdown
# <专题名称> 源码解析

> 版本基线：<JDK / 框架版本与固定 tag>

## 要解决的问题

- <真实工程问题>
- <读完后应该能解释的边界>

## 从哪里进入

1. `<公开 API>`：<入口意义>
2. `<核心方法>`：<关键分支>
3. `<状态收敛方法>`：<结果如何发布或清理>

## 核心数据结构

| 字段或节点 | 作用 | 需要观察的变化 |
| --- | --- | --- |
| `<field>` | <作用> | <何时变化> |

## 调用链

```text
公开 API
  -> 核心入口
    -> 分支选择
      -> 状态更新
```

## 关键流程

<按阶段解释源码意图、约束和设计取舍；必要时加入可暂停动画。>

## 推荐断点

| 方法 | 触发场景 | 观察变量 |
| --- | --- | --- |
| `<method>` | <最小触发条件> | `<variables>` |

## Lab 与测试

```bash
<可直接运行的 Maven 或 Gradle 命令>
```

<说明预期输出、测试断言和如何确认断点命中了目标分支。>

## 版本差异与边界

- <公开契约>
- <当前版本实现细节>
- <升级后必须重新验证的行为>

## 源码与参考

- [固定版本源码](<上游永久链接>)
- [官方 API 或规范](<官方地址>)
````

## 同步修改清单

新增专题不能只增加一篇 Markdown，还应同步检查：

- `source-index/` 中的唯一专题数据，包括入口、断点、Lab、证据链和下一站。
- `labs/` 中的最小案例与 JUnit 行为测试。
- `docs/.vitepress/config.mts` 中的导航和侧边栏。
- 学习路线、源码索引与必要的版本对比数据。
- 上游固定源码地址、许可证和适用版本说明。

提交前运行 `mvn --batch-mode test` 和 `npm run verify:docs`。Lab 的并发稳定性、断点设计与断言标准见 [Lab 编写规范](/guide/lab-authoring)。
